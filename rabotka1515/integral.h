#ifndef integral_H
#define integral_H

double function(double x, int s);
double sumofInt(double lbound, double ubound, int n, double dx, int s);
double sumofIntTrap(double lbound, double ubound, int n, double dx, int s);
double sumofIntSimp(double lbound, double ubound, int n, double dx, int s);

#endif

